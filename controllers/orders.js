// get all orders
exports.getOrders = (req, res, next) => {
  // .send(html)
  res.json({ message: "All orders." });
  // res.status(400).json({})
};

// Create
//app.post("/", (req, res) => {}

//Update
//app.put("/:id", (req, res) => {...res.params.id...}
